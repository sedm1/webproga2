version: '3.8'

services:
  webproga:
    build: .
    container_name: webproga_app
    restart: always
    expose:
      - "3000"
    networks:
      - aiphra-net

networks:
  aiphra-net:
    external: true